import { ColumnElevationRightDirective } from './column-elevation-right.directive';

describe('ColumnElevationRightDirective', () => {
  it('should create an instance', () => {
    const directive = new ColumnElevationRightDirective();
    expect(directive).toBeTruthy();
  });
});
