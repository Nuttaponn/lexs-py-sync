import { ColumnElevationLeftDirective } from './column-elevation-left.directive';

describe('ColumnElevationLeftDirective', () => {
  it('should create an instance', () => {
    const directive = new ColumnElevationLeftDirective();
    expect(directive).toBeTruthy();
  });
});
