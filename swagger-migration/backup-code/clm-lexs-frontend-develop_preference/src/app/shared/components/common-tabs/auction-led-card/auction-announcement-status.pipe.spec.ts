import { AuctionAnnouncementStatusPipe } from './auction-announcement-status.pipe';

describe('AuctionAnnouncementStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new AuctionAnnouncementStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
