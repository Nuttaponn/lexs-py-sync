import { DropDownConfig } from '@spig/core';

export const DEFAULT_DROPDOWN_CONFIG: DropDownConfig = {
  labelPlaceHolder: 'โปรดระบุ',
  displayWith: 'text',
  valueField: 'value',
  searchWith: 'text',
};
