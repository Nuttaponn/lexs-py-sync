import { CashierChequeStatusPipe } from './cashier-cheque-status.pipe';

describe('CashierChequeStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new CashierChequeStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
