import { AssetOwnerPipe } from './asset-owner.pipe';

describe('AssetOwnerPipe', () => {
  it('create an instance', () => {
    const pipe = new AssetOwnerPipe();
    expect(pipe).toBeTruthy();
  });
});
